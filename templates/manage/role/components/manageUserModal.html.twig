{# <div class="modal fade" id="manageUserModal" tabindex="-1" aria-labelledby="manageUserModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-xl">
    <div class="modal-content">
      <div class="modal-header align-items-center">
        <h1 class="modal-title fs-5" id="manageUserModalLabel">Manage Users</h1>
        <div class="d-flex align-items-center justify-content-end gap-2">
        <input class="form-control form-control-sm" type="search" placeholder="Filter" />
        <button class="btn btn-success btn-sm flex-shrink-0" data-bs-toggle="modal" data-bs-target="#addUserModal">Add Users</button>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
      </div>
      <div class="modal-body">
        ...
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="button" class="btn btn-primary">Save Changes</button>
      </div>
    </div>
  </div>
</div> #}

<div class="modal fade" data-bs-backdrop="static" data-bs-keyboard="false" id="addUserModal" tabindex="-1" aria-labelledby="addUserModalLabel" aria-hidden="true">
	<div class="modal-dialog modal-lg modal-dialog-scrollable">
		<div class="modal-content">
			<div class="modal-header align-items-center">
				<h1 class="modal-title fs-5" id="addUserModalLabel">Add Users</h1>
				<div class="d-flex align-items-center">
					<input class="form-control form-control-sm" type="search" placeholder="Filter"/>
				</div>
			</div>
			<div class="modal-body p-0" id="addUserToRole">
				...
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
				<button type="button" class="btn btn-primary" data-bs-dismiss="modal">Save Changes</button>
			</div>
		</div>
	</div>
</div>

<div class="modal fade" data-bs-keyboard="false" id="removeUserModal" tabindex="-1" aria-labelledby="removeUserModalLabel" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header align-items-center text-bg-danger">
				<h1 class="modal-title fs-5" id="removeUserModalLabel">Remove User</h1>
			</div>
			<form class="modal-body" id="removeUserForm" name="removeUserForm" action="">
        <p class="mb-0">Confirm that you want to remove <span id="targetName" class="fw-bold"></span> from the <span id="roleName" class="fw-bold"></span> role?</p>
        <input type="hidden" name="target">
        <input type="hidden" name="role">
			</form>
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
				<button type="submit" class="btn btn-danger" data-bs-dismiss="modal" form="removeUserForm">Remove</button>
			</div>
		</div>
	</div>
</div>

{{ webpack_entry_script_tags('manageRoleUsers') }}
